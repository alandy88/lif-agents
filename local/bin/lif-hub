#!/bin/sh
# Compatibility shim: the hub moved to lif-workbench (`lif-cli hub`).
if [ "${1-}" = "serve" ]; then
  echo "lif-hub serve was removed; run the workbench-lite service and use 'lif-hub open'" >&2
  exit 2
fi
exec lif-cli hub "$@"
